version: '3'
services:
  encryptor:
    build:
        context: ../../../.
        args:
          PROJECT_NAME: encrypt-api
  decryptor:
    build:
        context: ../../../.
        args:
          PROJECT_NAME: decrypt-api
